#include <iostream>
#include <example_lib/example_lib.h>
#include <header-only/header-only.h>

int main(int argc, char* argv[]) {
    std::cout << sum(10, 20) << "\n";
    std::cout << sub(10, 20) << "\n";
}
